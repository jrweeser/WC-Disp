# Set to 1 to use simulated data instead of the live API
USE_MOCK_DATA=0

# How often the background thread refreshes data (seconds)
FETCH_INTERVAL_SECONDS=60

# Optional: free token from https://www.football-data.org/client/register
# Enables possession, shots, fouls, and corners on live matches
FOOTBALL_DATA_API_KEY=
